class User {
  String id,firstName, lastName, email, phone, password, pictureUrl;
  bool isProfileCompleted, isVerified;
  // int ;
  User({
    this.id,
    this.firstName,
    this.lastName,
    this.email,
    this.phone,
    this.password,
    this.pictureUrl,
    this.isProfileCompleted,
    this.isVerified
  });
}
